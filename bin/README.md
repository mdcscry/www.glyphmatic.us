# Bin Scripts

Python operational scripts live in `bin/`.

For the paintings + artist grid workflow, see:
- `tools/paintings/README.md`

Compatibility symlinks may still exist in `tools/` and `_cron/` for older commands.
